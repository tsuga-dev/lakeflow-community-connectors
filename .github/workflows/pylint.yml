name: Pylint

# pull_request_target (instead of pull_request) is required so fork-PR
# runs receive the OIDC token that configure-jfrog-pypi exchanges for a
# JFrog Artifactory access token. With pull_request the fork's run gets
# no OIDC, and the action fails with ACTIONS_ID_TOKEN_REQUEST_TOKEN
# unbound.
#
# Untrusted code from fork PRs is gated by the repo's "Require approval
# for outside collaborators" setting — fork-PR workflow runs are pending
# until a maintainer explicitly approves them. The explicit head-SHA
# checkout below is what actually exercises the PR's code.
on:
  push:
    branches: [master]
    paths:
      - '**/*.py'
      - 'pyproject.toml'
      - 'requirements/**'
      - '.github/workflows/pylint.yml'
  pull_request_target:
    branches: [master]
    # Default types (opened, synchronize, reopened) plus `labeled` so a
    # maintainer's `safe-to-test` add fires CI on the exact labeled SHA.
    types: [opened, synchronize, reopened, labeled]
    # No workflow-level `paths:` filter on PR events: branch protection
    # requires `Pylint / build (3.10)`, so the workflow must fire on
    # every PR. Lint runs against the full source tree; PRs that touch
    # nothing lint-relevant still get a clean pass.

permissions:
  contents: read
  id-token: write  # Required for JFrog OIDC token exchange (see .github/actions/configure-jfrog-pypi).

jobs:
  build:
    runs-on:
      group: databrickslabs-protected-runner-group
      labels: linux-ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10"]
    steps:
    # First step (runs before any fork-controlled code is on disk).
    # Fork PRs only run CI when a maintainer adds the `safe-to-test`
    # label, and only on the exact event (`action == 'labeled' &&
    # label.name == 'safe-to-test'`) — a synchronize after labeling
    # does NOT pass. Explicit failure (rather than a job-level `if:`
    # skip) ensures branch protection sees `failure`, not the ambiguous
    # `skipped`. See CONTRIBUTING.md.
    - name: Authorize
      env:
        EVENT_NAME: ${{ github.event_name }}
        HEAD_REPO: ${{ github.event.pull_request.head.repo.full_name }}
        BASE_REPO: ${{ github.repository }}
        ACTION: ${{ github.event.action }}
        LABEL_NAME: ${{ github.event.label.name }}
      run: |
        if [ "$EVENT_NAME" = "push" ]; then
          echo "OK: push to master."
          exit 0
        fi
        if [ "$HEAD_REPO" = "$BASE_REPO" ]; then
          echo "OK: same-repo PR ($HEAD_REPO)."
          exit 0
        fi
        if [ "$ACTION" = "labeled" ] && [ "$LABEL_NAME" = "safe-to-test" ]; then
          echo "OK: fork PR was just labeled 'safe-to-test'."
          exit 0
        fi
        echo "::error title=Fork PR not authorized::Apply (or re-apply) the 'safe-to-test' label to authorize CI on this commit. See CONTRIBUTING.md."
        exit 1
    - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # v6.0.3
      with:
        # pull_request_target defaults to checking out the base ref; we want
        # to lint the PR's actual code. For push/same-repo events the empty
        # fallback resolves to the workflow ref.
        ref: ${{ github.event.pull_request.head.sha }}
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6.2.0
      with:
        python-version: ${{ matrix.python-version }}
    - name: Configure pip via JFrog proxy
      uses: ./.github/actions/configure-jfrog-pypi
    - name: Install dependencies
      run: |
        LOCK="requirements/pylint.txt"
        if [ ! -f "$LOCK" ]; then
          echo "::error title=Missing lock::$LOCK is required. Run tools/scripts/regenerate-locks.sh and commit requirements/."
          exit 1
        fi
        pip install -r "$LOCK"
        pip install -e . --no-deps
        pip install -e ./tools/community_connector --no-deps
    - name: Analysing the code with pylint
      run: |
        # Lint source files (excluding tests directory and generated files).
        #
        # Disabled checks:
        #   W       — warnings (pre-existing project policy)
        #   C0114   — missing-module-docstring
        #   C0115   — missing-class-docstring
        #   C0116   — missing-function-docstring (noisy on dunders, factories,
        #             one-line helpers; module/class docstrings already cover intent)
        #   C0415   — import-outside-toplevel (deliberate lazy imports for
        #             circular-dep avoidance and to defer optional cost)
        #   R0801   — duplicate-code
        #   R0902   — too-many-instance-attributes (Simulator/EndpointSpec/
        #             LiveValidator are config-bag dataclasses)
        #   R0903   — too-few-public-methods (single-purpose helpers are fine)
        #   R0911   — too-many-return-statements (guard-clause pattern)
        #   R0913   — too-many-arguments (named kwargs on stable APIs)
        #   R0917   — too-many-positional-arguments
        #   R1705   — unnecessary-else-after-return
        pylint \
        --max-line-length=100 \
        --max-locals=25 \
        --max-branches=20 \
        --max-module-lines=1500 \
        --disable=W,C0114,C0115,C0116,C0415,R0801,R0902,R0903,R0911,R0913,R0917,R1705 \
        --ignore-long-lines='^\s*(#|f?".*"|f?'\''.*'\'')$' \
        $(git ls-files '*.py' | grep -v '_generated_' | grep -v 'pipeline-spec/example_ingest.py' | grep -v 'pipeline_spec.example.py' | grep -v 'tools/scripts/merge_python_source.py' | grep -v 'community_connector_cli/templates/ingest_template' | grep -v 'community_connector/sources/hubspot/' | grep -v 'community_connector/sources/stripe/' | grep -v 'community_connector/sources/mixpanel/' | grep -v 'microsoft_teams/sample-ingest.py' | grep -v 'tsuga_logs/sample-ingest.py' | grep -v '^tests/')
